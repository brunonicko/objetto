TODO:
  - Ability to lock/unlock an object (recursively or not) and give it a "reason" message
  - Serialization design, ability to serialize/deserialize to/from frozen if desired
  - Application properties/global variables or how to retrieve objects from registry
  - Object classes (Object, ContainerObject, ListObject, DictObject)
  - Object observer class/mixin
  - History object class
  - Logging object classes
  - Status object classes
  - Knob object classes

DONE:
  - Application 'context_safe' parameter
  - Freezable relationship support
